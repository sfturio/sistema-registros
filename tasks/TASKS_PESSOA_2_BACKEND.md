# Pessoa 2 - Backend (Node)

## Tarefa mais importante hoje
1. **Definir e publicar o contrato da API de egistros (payload, resposta e status codes).**

## Objetivo
Fechar o contrato da API e garantir um backend estavel para o MVP.

## Importante hoje (primeira tarefa)
1. **Definir e publicar o contrato da API de `registros` (payload, resposta e status codes) e compartilhar com Pessoa 1 e Pessoa 3.**

## Tarefas
2. Validar endpoints do CRUD (`GET`, `POST`, `PUT`, `DELETE`).
3. Revisar validacao de payload e mensagens de erro.
4. Garantir consistencia de response (`ok`, `data`, `error`).
5. Adicionar tratamento de erro global e logs minimos.
6. Publicar colecao de testes (Postman/Insomnia) para o time.

## Entregaveis
- Contrato de API documentado no `docs/`.
- API CRUD validada localmente.
- Exemplo de requests/responses para o frontend.

## Dependencias
- Banco e schema da Pessoa 3 para validacao final.

