# SistemaRegistrosMVC

## Como usar
1. Copie `.env.example` para `.env` em cada backend.
2. Atualize credenciais do PostgreSQL.
3. Inicie cada camada separadamente.

## Endpoints iniciais
- Node: `GET /registros`
- .NET: `GET /api/registros`
